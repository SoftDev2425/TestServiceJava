<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Puppy Crawl//DTD Check Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">

    <!-- Simple configuration: only essential checks -->
    <module name="TreeWalker">

        <!-- Ensure there are no unused imports -->
        <module name="UnusedImports"/>

        <!-- Enforce method names are in lowerCamelCase -->
        <module name="MethodName"/>

        <!-- Ensure each file ends with a newline -->
        <module name="NewlineAtEndOfFile"/>

        <!-- Basic design rule for methods and classes -->
        <module name="DesignForExtension"/>

    </module>

    <!-- Ensure imports are ordered alphabetically -->
    <module name="ImportOrder">
        <property name="order" value="alpha"/>
    </module>

    <!-- Limit line length to a reasonable number -->
    <module name="LineLength">
        <property name="max" value="120"/>
    </module>

</module>
