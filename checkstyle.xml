<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <module name="TreeWalker">
        <!-- Enforce naming conventions for class names -->
        <module name="TypeName">
            <property name="format" value="^[A-Z][a-zA-Z0-9]*$"/>
        </module>

        <!-- Enforce naming conventions for method names -->
        <module name="MethodName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>

        <!-- Enforce naming conventions for variable names -->
        <module name="LocalVariableName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>

        <!-- Prevent use of magic numbers -->
        <module name="MagicNumber"/>

        <!-- Ensure methods do not exceed a maximum number of lines -->
        <module name="MethodLength">
            <property name="max" value="50"/>
        </module>

        <!-- Check for unused imports -->
        <module name="UnusedImports"/>

        <!-- Enforce consistent use of braces for if/else/for/while -->
        <module name="NeedBraces"/>
    </module>
</module>